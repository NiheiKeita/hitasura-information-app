enum Category {
  pseudocodeExecution,
  controlFlowTrace,
  binaryToDecimal,
  decimalToBinary,
  binaryMixed,
}

enum PracticeMode {
  infinite,
  timeAttack10,
}

enum Difficulty {
  easy,
  normal,
  hard,
}
